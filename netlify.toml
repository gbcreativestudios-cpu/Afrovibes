[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (e.g. /events, /product/round-neck)
# don't 404 on a hard refresh or direct link.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
